# Development dependencies for Agentic Loop.
# Install alongside runtime/requirements.txt:
#   .venv/Scripts/pip install -r runtime/requirements.txt -r runtime/requirements-dev.txt

ruff>=0.5.0
black>=24.0
mypy>=1.10
pytest-cov>=5.0
pre-commit>=3.7
hypothesis>=6.100
